name: Publish Plugin

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: read

jobs:
  publish:
    name: Publish to JetBrains Marketplace
    runs-on: ubuntu-latest
    if: vars.PUBLISH_PLUGIN == 'true'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: gradle

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Derive version
        id: ver
        shell: bash
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Build plugin
        shell: bash
        run: ./gradlew :recordrelay-plugin:buildPlugin "-PreleaseVersion=${{ steps.ver.outputs.VERSION }}"

      - name: Sign plugin
        shell: bash
        env:
          CERTIFICATE_CHAIN: ${{ secrets.CERTIFICATE_CHAIN }}
          PRIVATE_KEY: ${{ secrets.PRIVATE_KEY }}
          PRIVATE_KEY_PASSWORD: ${{ secrets.PRIVATE_KEY_PASSWORD }}
        run: |
          if [ -n "$CERTIFICATE_CHAIN" ]; then
            ./gradlew :recordrelay-plugin:signPlugin "-PreleaseVersion=${{ steps.ver.outputs.VERSION }}"
          else
            echo "Signing credentials not configured, skipping."
          fi

      - name: Publish plugin
        shell: bash
        env:
          PUBLISH_TOKEN: ${{ secrets.PUBLISH_TOKEN }}
        run: ./gradlew :recordrelay-plugin:publishPlugin "-PreleaseVersion=${{ steps.ver.outputs.VERSION }}"
