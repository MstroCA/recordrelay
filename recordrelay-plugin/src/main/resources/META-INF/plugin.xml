<idea-plugin>
    <id>io.recordrelay.plugin</id>
    <name>RecordRelay</name>
    <vendor>RecordRelay Authors</vendor>
    <description>
        Clone a database record and its full FK-linked context from one environment to another —
        source, target, ID, done. Supports PostgreSQL, MySQL, SQL Server, Oracle, MongoDB and more.
        Includes PII masking, field overrides, schema discovery, and export packages.
        Compatible with IntelliJ IDEA 2024.1 and later.
    </description>

    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">
        <toolWindow id="RecordRelay"
                    anchor="right"
                    factoryClass="io.recordrelay.plugin.toolwindow.RecordRelayToolWindowFactory"
                    canCloseContents="false"/>
        <applicationService
                serviceImplementation="io.recordrelay.plugin.service.RecordRelayService"/>
    </extensions>

</idea-plugin>
